spring.jpa.hibernate.ddl-auto=validate
#validate: validate the schema, makes no changes to the database.
#update: update the schema.
#create: creates the schema, destroying previous data.
#create-drop: drop the schema when the SessionFactory is closed explicitly, typically when the application is stopped.
#none: does nothing with the schema, makes no changes to the database
spring.jpa.show-sql=false

spring.freemarker.expose-request-attributes=true

#server.servlet.context-path=/LabAssist/


#FOR mySQL production:
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/lab_assist?serverTimezone=Europe/Minsk
spring.datasource.username=LabAssist
spring.datasource.password=qwerty007_ADMIN
spring.jpa.database-platform=org.hibernate.dialect.MySQL55Dialect

server.servlet.session.timeout=120m

#FOR postgres deploy:
#spring.datasource.url=${SPRING_DATASOURCE_URL}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

#server.servlet.session.timeout=30m

